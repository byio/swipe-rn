import React, { Component } from 'react';
import { View } from 'react-native';

class Ball extends Component {
  // render method
  render () {
    return (
      <View />
    );
  }
}

export default Ball;
